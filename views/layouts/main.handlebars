<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/css/bulma.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Yellowtail&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bulma-carousel@4.0.4/dist/js/bulma-carousel.min.js"></script>
    <script src="https://kit.fontawesome.com/58afa72070.js" crossorigin="anonymous"></script>
</head>

<body>
  <nav class="navbar is-dark" role="navigation" aria-label="main navigation">
      <div class="navbar-brand">
        <h1 class="navbar-item Yellowtail">Tech Blog</h1>
    
        <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
        </a>
      </div>
    
      <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-start">
          <a href="/" class="navbar-item">Home</a>
          <a href="/dashboard" class="navbar-item">My Dashboard</a>
        </div>
   
        <div class="navbar-end">
          <div class="navbar-item">
            <div class="buttons">
              {{!-- needs to be a route --}}
              {{#if loggedIn}}
              <a id='logButton' class="button is-danger">Log Out</a>
              <a id='newPostButton' class="button is-success">New Post</a>
                {{else}}
              <a class="button is-light" href="/login">Log In</a>
              {{/if}}
            </div>
          </div>
        </div>
      </div>
    </nav>
<div class="modal" id="modal">
      <div class="modal-background"></div>
      <div class="modal-content box">
         <form action="#" method="post" id="posting">
            <div class="field is-clipped">
            <label class="label" for="subject">Subject</label>
            <div class="control">
               <input class="input" id="subjectText" type="text" name="subject" placeholder="Subject">
            </div>
            <p id="subjectHelp" class="help is-danger is-hidden">Subject cannot be empty</p>
            </div>
            <div class="field">
            <label class="label" for="">Body</label>
            <div class="control">
               <textarea id="bodyText" class="textarea" name="body" rows="3" placeholder="Write something..." id="body"></textarea>
            </div>
            <p id="bodyHelp" class="help is-danger is-hidden">The Body of your post cannot be empty</p>
            </div>
              <a id='cancelPost' class="button is-danger">Close</a>
              <a id='postThis' class="button is-success">Post</a>

         </form>
      </div>
      <button class="modal-close is-large" onClick={toggleModal()} aria-label="close"></button>
      </div>
    <div class="modal" id="commentModal">
      <div class="modal-background"></div>
      <div class="modal-content box">
         <form action="#" method="post" id="comment">
            <div class="field is-clipped">
            <label class="label" for="subject">Subject</label>
            <div class="control">
               <input class="input" readonly id="commentSubjectText" type="text" name="subject" placeholder="Subject">
            </div>
            </div>
            <div class="field">
            <label class="label" for="">Comment</label>
            <div class="control">
               <textarea id="commentText" class="textarea" name="comment" rows="3" placeholder="Comment..." id="body"></textarea>
            </div>
            <p id="commentHelp" class="help is-danger is-hidden">Your comment cannot be empty</p>
            </div>
              <a id='cancelComment' class="button is-danger">Close</a>
              <a id='commentThis' class="button is-success">Post</a>

         </form>
      </div>
      <button class="modal-close is-large" onClick={toggleModal()} aria-label="close"></button>
      </div>
      <div class="modal" id="editModal">
      <div class="modal-background"></div>
      <div class="modal-content box">
         <form action="#" method="post" id="edit">
            <div class="field is-clipped">
            <label class="label" for="subject">Subject</label>
            <div class="control">
               <input class="input" id="editSubjectText" type="text" name="subject" placeholder="Subject">
            </div>
            <p id="editSubjectHelp" class="help is-danger is-hidden">Subject cannot be empty</p>
            </div>
            <div class="field">
            <label class="label" for="">Body</label>
            <div class="control">
               <textarea id="editBodyText" class="textarea" name="body" rows="3" placeholder="Write something..." id="body"></textarea>
            </div>
            <p id="editBodyHelp" class="help is-danger is-hidden">The Body of your post cannot be empty</p>
            </div>
              <a id='cancelEdit' class="button is-danger">Close</a>
              <a id='saveThis' class="button is-success">Save</a>

         </form>
      </div>

      <button class="modal-close is-large" onClick={toggleModal()} aria-label="close"></button>
   </div>
{{{body}}}

  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="/js/logout.js"></script>
  {{#if loggedIn}}
  <script src="/js/posts.js"></script>
  {{/if}}
  <script src="/js/script.js"></script>
</body>
  
</html>